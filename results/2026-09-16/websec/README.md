# Websec sample — 2026-09-16

Read-only `websec run <clone>` (no `--scan`, no live probes) on two public repos already in the DocGuard corpus.

| Repo | Notes |
|---|---|
| `pallets/click` | Recon briefing + staged probe templates only |
| `encode/httpx` | Recon briefing + staged probe templates only |

Constructive dual-signal with DocGuard scorecards. No upstream source republished.
